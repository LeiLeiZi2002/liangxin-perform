# 交互界面

这里是心理工作者情境测评的 React 前端，包含任务配置、服务检查、实时语音或文字会谈、工作记录和报告页面。项目的整体说明、环境准备和启动方式见根目录 `README.md`。

## 常用命令

```bash
npm run dev
npm test -- --run
npm run lint
npm run build
```

前端不保存阿里云百炼 API Key。设置页提交后，凭证只保留在当前后端进程内存中；刷新页面不会泄露凭证，后端重启后需重新填写。
